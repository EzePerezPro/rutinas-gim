export type rutina = {
    ejercicios: [
        {
            nombre: string;
            descripcion: string;
            materiales: string;
            peso_estimado: string;
            repeticiones: string;
            series: string;
        }
    ],
};